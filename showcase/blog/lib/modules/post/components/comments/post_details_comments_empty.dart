import 'package:flutter/material.dart';

/// The empty state for the PostDetailsComments page.
/// Generated as a starting point — customize freely, or delete this file
/// and its use in the page to fall back to AG's default empty widget.
class PostDetailsCommentsEmpty extends StatelessWidget {
  const PostDetailsCommentsEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No comments yet.'));
  }
}
