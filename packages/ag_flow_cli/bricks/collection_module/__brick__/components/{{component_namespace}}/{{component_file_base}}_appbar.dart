import 'package:flutter/material.dart';

/// The app bar for the {{component_class_prefix}} page.
/// Generated as a starting point — customize freely, or delete this file
/// and its use in the page to fall back to no app bar.
class {{component_class_prefix}}AppBar extends AppBar {
  {{component_class_prefix}}AppBar({super.key})
    : super(title: const Text('{{module_class_prefix}}'));
}
