import 'package:flutter/material.dart';

/// The empty state for the {{component_class_prefix}} page.
/// Generated as a starting point — customize freely, or delete this file
/// and its use in the page to fall back to AG's default empty widget.
class {{component_class_prefix}}Empty extends StatelessWidget {
  const {{component_class_prefix}}Empty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Not found.'));
  }
}
